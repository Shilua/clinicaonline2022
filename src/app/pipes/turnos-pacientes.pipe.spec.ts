import { TurnosPacientesPipe } from './turnos-pacientes.pipe';

describe('TurnosPacientesPipe', () => {
  it('create an instance', () => {
    const pipe = new TurnosPacientesPipe();
    expect(pipe).toBeTruthy();
  });
});
